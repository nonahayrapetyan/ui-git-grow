import { ButtonProps } from 'antd/es/button';

export type TButtonProps = ButtonProps;

export enum EButtonTypes {
  Primary = 'primary',
  Default = 'default',
  Dashed = 'dashed',
  Text = 'text',
  Ghost = 'ghost',
  Link = 'link',
}
